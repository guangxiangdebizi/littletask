import { StyleSheet } from 'react-native';

import { colors, radii, spacing } from '../theme/tokens';

export const intakeResultStyles = StyleSheet.create({
  topSpacing: { paddingTop: spacing[8] },
  summary: {
    gap: spacing[3],
    paddingBottom: spacing[6],
    paddingTop: spacing[8],
  },
  kicker: { color: colors.pine, fontSize: 13, fontWeight: '700' },
  summaryTitle: {
    color: colors.ink,
    fontSize: 26,
    fontWeight: '700',
    letterSpacing: 0,
    lineHeight: 34,
  },
  summaryMeta: {
    alignItems: 'center',
    flexDirection: 'row',
    flexWrap: 'wrap',
    gap: spacing[2],
  },
  summaryMetaText: { color: colors.muted, fontSize: 13 },
  metaDot: {
    backgroundColor: colors.lineStrong,
    borderRadius: 2,
    height: 4,
    width: 4,
  },
  modeNotice: {
    alignItems: 'flex-start',
    backgroundColor: colors.blueSoft,
    borderRadius: radii.md,
    flexDirection: 'row',
    gap: spacing[2],
    padding: spacing[3],
  },
  modeNoticeText: { color: colors.ink, flex: 1, fontSize: 13, lineHeight: 19 },
  questionsSection: { gap: spacing[3], paddingTop: spacing[8] },
  question: {
    backgroundColor: colors.amberSoft,
    borderRadius: radii.md,
    gap: spacing[2],
    padding: spacing[4],
  },
  questionPrompt: { color: colors.ink, fontSize: 15, fontWeight: '700' },
  questionOptions: { color: colors.amber, fontSize: 12, lineHeight: 18 },
  actionsSection: { gap: spacing[4], paddingTop: spacing[8] },
  sectionHeadingRow: {
    alignItems: 'baseline',
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  sectionTitle: {
    color: colors.ink,
    fontSize: 20,
    fontWeight: '700',
    letterSpacing: 0,
  },
  sectionHint: { color: colors.faint, fontSize: 12 },
  emptyState: {
    alignItems: 'center',
    backgroundColor: colors.surface,
    borderColor: colors.line,
    borderRadius: radii.lg,
    borderWidth: 1,
    gap: spacing[2],
    padding: spacing[6],
  },
  emptyTitle: { color: colors.ink, fontSize: 16, fontWeight: '700' },
  emptyBody: {
    color: colors.muted,
    fontSize: 13,
    lineHeight: 19,
    textAlign: 'center',
  },
  insightsSection: {
    backgroundColor: colors.surface,
    borderColor: colors.line,
    borderRadius: radii.lg,
    borderWidth: 1,
    marginTop: spacing[8],
    paddingHorizontal: spacing[4],
    paddingTop: spacing[4],
  },
  insightHeading: { gap: spacing[1] },
  loadingInsights: { color: colors.muted, fontSize: 14, paddingVertical: spacing[5] },
  errorState: {
    alignSelf: 'center',
    gap: spacing[3],
    maxWidth: 480,
    paddingTop: spacing[10],
    width: '100%',
  },
  errorIcon: {
    alignItems: 'center',
    backgroundColor: colors.coralSoft,
    borderRadius: radii.md,
    height: 48,
    justifyContent: 'center',
    width: 48,
  },
  errorTitle: { color: colors.ink, fontSize: 24, fontWeight: '700' },
  errorBody: {
    color: colors.muted,
    fontSize: 15,
    lineHeight: 22,
    marginBottom: spacing[2],
  },
});
